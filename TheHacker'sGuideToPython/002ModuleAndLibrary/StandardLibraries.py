'''
Python中自带了一些标准库,建议要对Python标准库要比较熟悉,因为标准库的性能一般比自己写的更加好
所以如果标准库中有的方法,就要尽量使用标准库里的方法
'''

import atexit  # 允许注册程序退出时调用的函数
import argparse  # 提供解析命令行参数的函数
import bisect  # 为可排序列表提供二分查找算法
import calendar  # 提供一组与日期相关的函数
import codecs  # 提供字符编码数据的函数
import collections  # 提供一组有用的数据结构
import copy  # 提供复制数据的函数
import csv  # 提供用于读写csv文件的函数
import datetime  # 提供用于处理日期和时间的类
import tempfile  # 建立临时文件
import fnmatch # 提供用于匹配
import re  # 正则表达式

help(re)



